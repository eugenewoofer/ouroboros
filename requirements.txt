openai>=1.66.0
requests
# socks/http proxy support for httpx (used by web_search via OPENAI_HTTPS_PROXY)
socksio
dulwich
starlette
python-multipart
uvicorn[standard]
websockets
huggingface_hub
claude-agent-sdk>=0.1.50
# optional: browser automation tools (for browse_page/browser_action)
playwright
playwright-stealth
# optional: local model server (requires Metal-enabled build on macOS)
# CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python[server]
